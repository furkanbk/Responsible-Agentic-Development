<!-- RADF HW1 — every PR fills this in (CLAUDE.md §2). -->

## What this closes
- TODO item(s): <!-- e.g. T0.6 -->
- Owner: <!-- your name; you may only edit files you own (CLAUDE.md §1) -->

## Files touched
<!-- list every file; do not touch files owned by someone else -->

## Contract changes
- [ ] This PR changes a contract in `ARCHITECTURE.md` (stub signature, `Result`
      fields, or the graph JSON schema). If checked, the contract owner is a
      required reviewer and the change was agreed first.
- [ ] No contract changed.

## ARCHITECTURE.md
- [ ] Updated the relevant component entry, public contract, and a decision
      record for anything non-obvious (CLAUDE.md §3). **A PR that adds/changes a
      component without this is incomplete.**

## Checks
- [ ] No new dependency outside CLAUDE.md §4 (stdlib, `openai`, `python-dotenv`, `pytest`).
- [ ] No `.env`, API keys, `store/*.json`, or notebook outputs committed.
- [ ] Stayed inside my ownership boundary; stub signatures/docstrings/return shapes unchanged.
- [ ] At least one teammate approving; the owner of any contract I consume is a reviewer.
