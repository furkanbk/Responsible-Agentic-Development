# RADF HW1 — deliberately minimal. No agent/LLM framework (CLAUDE.md §4).
openai>=1.40.0
python-dotenv>=1.0.0

# HW4 — framework refactor, CLAUDE.md §4 HW4 amendment (decision #53). Floor-pinned; no lockfile.
langgraph>=1.2.10
langchain>=1.3.14
langchain-openai>=1.4.1

# HW5 — retrieval layer, CLAUDE.md §4 HW5 amendment (decision #56).
# psycopg is the pgvector client; the vectors themselves come from OpenRouter over
# plain HTTP, so there is no embedding SDK here. BM25, RRF and the reranker are
# hand-written (decisions #58, #59) — no rank_bm25, no vector-store SDK, and no
# eval library: Ragas and DeepEval both stay out (§4 amendment).
psycopg[binary]>=3.1

# HW6 — tracing, CLAUDE.md §4 HW6 amendment (decision #70). The trace store and
# the span tree come from here; the trajectory adapter, the five agent metrics
# and every safety detector stay hand-written. Full `mlflow`, not `mlflow-skinny`:
# skinny has no SQLAlchemy backend, and MLflow 3 refuses the `./mlruns` file store
# outright, so skinny leaves nowhere to put a trace. `mlflow ui` also needs it.
mlflow>=3.0

# Tests only
pytest>=8.0.0
