# RADF HW1 — deliberately minimal. No agent/LLM framework (CLAUDE.md §4).
openai>=1.40.0
python-dotenv>=1.0.0

# HW4 — framework refactor, CLAUDE.md §4 HW4 amendment (decision #53). Floor-pinned; no lockfile.
langgraph>=1.2.10
langchain>=1.3.14
langchain-openai>=1.4.1

# Tests only
pytest>=8.0.0
